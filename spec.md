# Daily 要件定義・設計仕様書

## 1. プロジェクト概要

| 項目 | 内容 |
| --- | --- |
| アプリ名 | Daily |
| 目的 | 面倒くさがりのための、必要な情報だけを簡単に記録・確認できるカレンダー/手帳風アプリ |
| 形式 | PWA（ホーム画面追加可能、オフライン起動対応） |
| 対象デバイス | iPhone（Safari）, PC（Chrome） |
| デザイン | ダーク・ミニマル・Apple/Linearライク |
| データ管理 | Supabase（無料プラン、メールアドレス認証あり、RLS適用） |

---

## 2. システム構成

- **F/E:** HTML5 / CSS3 / JavaScript (Vanilla JS, ES6+)
- **B/E:** PostgreSQL (Supabase)
- **Auth:** Supabase Auth (Email / Password)
- **外部API:** Open-Meteo API（天気情報取得）
- **祝日データ:** app.js 内埋め込み（2026〜2027年対応）

---

## 3. DB仕様（Supabase）

> 全テーブルで Row Level Security (RLS) を有効化し、`auth.uid() = user_id` のポリシーを設定。

### 3.1 events テーブル（予定・メモ）

| カラム名 | 型 | 制約 | 説明 |
|---|---|---|---|
| id | uuid | PK, gen_random_uuid() | 予定ID |
| user_id | uuid | NOT NULL, auth.uid() | ユーザーID |
| date | date | NOT NULL | 予定の日付 (YYYY-MM-DD) |
| text | text | NOT NULL | 予定内容 |
| created_at | timestamptz | DEFAULT now() | 作成日時 |

### 3.2 work_records テーブル（日々の勤務形態）

| カラム名 | 型 | 制約 | 説明 |
|---|---|---|---|
| id | uuid | PK, gen_random_uuid() | 記録ID |
| user_id | uuid | NOT NULL, auth.uid() | ユーザーID |
| date | date | NOT NULL | 対象日付 (YYYY-MM-DD) |
| status | text | NOT NULL, CHECK制限 | 勤務形態コード |
| updated_at | timestamptz | DEFAULT now() | 更新日時 |

- **CHECK値:** `office`, `remote`, `paid_full`, `paid_am`, `paid_pm`, `holiday_work`, `off`
- **複合UNIQUE制約:** `(user_id, date)`

### 3.3 settings テーブル（ユーザー設定）

| カラム名 | 型 | 制約 | 説明 |
|---|---|---|---|
| id | uuid | PK, gen_random_uuid() | 設定ID |
| user_id | uuid | NOT NULL, UNIQUE, auth.uid() | ユーザーID |
| data | jsonb | NOT NULL | 設定データオブジェクト |

- **初期データ生成:** `handle_new_user()` トリガーにより新規登録時に自動作成
- **data 構造:**

```json
{
  "workDefaults": {
    "mon": "remote", "tue": "office", "wed": "office",
    "thu": "remote", "fri": "remote", "sat": "off", "sun": "off"
  },
  "garbageSchedule": [
    { "type": "燃えるゴミ", "weekdays": [1, 4], "weeks": [] },
    { "type": "プラスチック", "weekdays": [6], "weeks": [] },
    { "type": "資源", "weekdays": [2], "weeks": [] },
    { "type": "燃えないゴミ", "weekdays": [5], "weeks": [2, 4] }
  ],
  "weatherLocation": { "name": "東京", "lat": 35.68, "lon": 139.76 }
}
```

---

## 4. 画面仕様

### ① 認証画面 (`#screen-auth`)

- メールアドレス・パスワード入力フォーム
- 「ログイン」および「新規登録」ボタン
- エラー・通知メッセージ表示領域
- 認証成功時、自動的にメインアプリ画面を表示

### ② メイン画面 (`#screen-today`)

- **ヘッダー:** アプリ名「Daily」、今日の日付（月・日・曜日）
- **天気カード:** 天気アイコン（44px、種別ごとに色分けした塗りつぶしデザイン）、最高気温、天気説明（Open-Meteo APIより取得）
- **服装アドバイス:** 傘要否（降水確率≥50%、必要時は赤背景で強調表示）、服装目安（最高気温より判定）
- **勤務形態カード:** 当日の勤務形態バッジ（タップで変更モーダル）＋ 今週の残り出社日数インジケーター
  （今日以降でまだ来ていない `office`/`holiday_work` の日数。2日以上＝赤、1日＝黄、0日＝緑）
- **ゴミの日カード:** 当日出すゴミの種類（対象なし時は「ゴミなし」）
- **予定カード:** 当日の予定リスト（タップで編集モーダル：テキスト編集・削除）
- **今週の残り予定カード:** 今日より後〜今週末（日曜）までの予定を日付バッジ付きで一覧表示
  （月境界をまたぐ週でもDBから直接取得するため正しく表示される）
- **FAB（＋）:** 画面右下固定、タップで予定追加モーダル表示（今日固定）

### ③ カレンダー画面 (`#screen-calendar`)

- **月ナビゲーション:** 「前月」「次月」ボタン、「今日」移動ボタン
- **カレンダーグリッド:** 当月の日付グリッド
  - 青ドット＝予定あり
  - 黄ドット＝`work_records`に明示的な記録があり当日デフォルトから変更されている
  - 紫ドット＝ゴミの日
- **選択日の詳細:** 日付タイトル、勤務形態ドロップダウン（リアルタイム変更可）、ゴミ種別バッジ、
  該当日の予定リスト、「予定を追加」ボタン（選択中の日付に対して予定を追加。モーダルタイトルに
  対象日付を表示し、今日以外への追加時に誤解しないようにする）

### ④ 設定画面 (`#screen-settings`)

- **勤務形態デフォルト:** 月〜日の曜日ごとのデフォルト勤務形態（タップで変更モーダル）
- **ゴミの日設定:** ゴミ種別ごとの曜日・週指定一覧表示
- **天気設定:** 取得地点（場所名、緯度、経度）の編集モーダル呼び出し
- **アプリ情報:** 「最新版を確認」ボタン（Service Workerのキャッシュを破棄し再読み込み。
  デプロイ直後に変更を即座に反映させたいときに使用）
- **アカウント:** ログアウトボタン

> ※ アプリ内蔵の通知設定UIはV1スコープ外。朝のサマリー通知はSupabase Edge Function +
> iOSショートカットの組み合わせで別途実現している（6章「プッシュ通知」参照）。

---

## 5. 機能仕様・判定ロジック

### 5.1 勤務形態の判定フロー

1. 対象日付で `work_records` を検索 → 記録があればその `status` を採用
2. 記録がない場合、祝日リスト（HOLIDAYS）に存在するかチェック → 祝日なら `off`
3. 祝日でもない場合、`settings.workDefaults` の該当曜日の設定値を採用

> 「今週の残り出社日数」（`getRemainingOfficeDaysThisWeek()`）は表示中の月データ
> （`workRecordsCache`）に依存せず、今週（月〜日）の `work_records` を都度DBから個別取得して
> 判定する。週が月境界をまたぐ場合でも正しくカウントするため。カウント対象は「今日以降」
> （今日を含む）の `office`/`holiday_work` の日のみで、今日より前の日は含めない。
> 色分け: 2日以上＝赤、1日＝黄、0日＝緑（デフォルト）。

### 5.2 ゴミの日判定ロジック

- 対象日付の「曜日番号(0=日〜6=土)」および「その月の第何回目の曜日か」を算出
  - 算出式: `floor((date - 1) / 7) + 1`
- `garbageSchedule` の各条件をチェック:
  - `weekdays` に該当曜日が含まれるか
  - `weeks` が空（毎週）、または算出された第N回目が含まれるか
- 両方を満たすゴミ種別を配列として抽出し表示

### 5.3 服装判定ロジック

| 条件 | 表示 |
|---|---|
| 最高気温 ≥ 28°C | 半袖で快適 |
| 最高気温 ≥ 22°C | 半袖＋薄手の上着 |
| 最高気温 ≥ 18°C | 長袖シャツで快適 |
| 最高気温 ≥ 12°C | 長袖＋カーディガン |
| 最高気温 < 12°C | コートが必要 |
| 降水確率 ≥ 50% | 傘必要 |

---

## 6. 非機能・運用仕様

### オフライン挙動（V1制約）

- Service Worker により静的ファイル（HTML/CSS/JS/アイコン）をローカルキャッシュ
- オフライン下でもアプリの起動・閲覧が可能
- オフライン中の書き込み操作はV1では制限し、トースト通知（「オフラインのため変更できません」）を表示
- Service Workerはキャッシュ優先で配信するため、デプロイ後も端末側が古いキャッシュを表示し
  続けることがある。設定画面の「最新版を確認」でキャッシュを破棄し強制的に再取得できる
  （開発側は、コード変更を伴うデプロイのたびに `service-worker.js` の `CACHE_NAME` を
  インクリメントする運用ルールとしている。詳細は README.md 参照）

### 祝日データの更新管理

- `app.js` 内の HOLIDAYS オブジェクトに埋め込み
- 年1回（年末年始等）に翌々年分を手動追加更新
- 同じ内容を Supabase Edge Function `daily-summary`（後述）側にも保持しているため、
  更新時は両方を同期させる（詳細手順は README.md 参照）

### プッシュ通知（朝のサマリー、V1.1で追加）

- Supabase Edge Function `daily-summary` が、当日の天気・服装アドバイス・勤務形態・
  ゴミの種類・予定をまとめたテキストをJSON形式（`body`キー等）で返すAPIエンドポイントを提供
- iPhoneの「ショートカット」アプリの個人用オートメーション（時刻トリガー）からこの
  エンドポイントを呼び出し、「通知を表示」でローカル通知として表示する構成
- OneSignal等の外部プッシュ通知サービスは使用しない（アプリからのWeb Push実装もV1.1では未対応）
- 認可は秘密トークン方式（URLクエリパラメータ `token`）。対象は固定の1ユーザーのみ
  （`TARGET_USER_ID` をEdge Function内にハードコード）で、複数ユーザー対応はしていない
- Edge Functionのソースはこの `daily` リポジトリには含まれない。Service Role Key相当の
  強い権限（RLSを経由せず全ユーザーのデータにアクセス可能）で動作するため、
  Supabaseダッシュボード側でのみ管理する。設定手順・通知文言の変更手順は README.md 参照

### データ取得・セキュリティ上の実装ルール

- 月次データ取得（`events` / `work_records`）の期間終端は、対象月の実日数
  （`new Date(year, month + 1, 0).getDate()`）から算出する。固定値 `31` を用いると
  2/4/6/9/11月で存在しない日付になりクエリが失敗するため使用しない。
- RLS（`auth.uid() = user_id`）に加え、`settings` テーブルの取得クエリでも
  `user_id = 現在のユーザーID` を明示条件として付与する（他のテーブル操作と実装を統一）。
- DBから取得した文字列（ゴミ種別 `garbageSchedule[].type` 等）をHTMLへ描画する箇所は、
  ユーザー入力由来の予定テキストと同様に必ず `escapeHtml()` を通す。
- オフライン判定（`navigator.onLine`）だけでなく、DB操作がエラーになった場合は
  「（通信状態をご確認ください）」を付記したトーストで通信起因の失敗であることを明示する。

---

## 7. ファイル構成

```
daily-app/
├── index.html          … 認証および主要3画面のHTML構造・モーダルUI
├── style.css           … ダークテーマスタイリング、CSS変数、レスポンシブ対応
├── app.js              … Supabase Auth、データ操作、画面制御、判定ロジック
├── manifest.json       … PWAマニフェスト（アイコン・テーマカラー・表示モード設定）
├── service-worker.js   … 静的アセットキャッシュ、オフライン表示制御
└── icons/
    ├── icon-192.png    … PWA用アイコン (192x192)
    └── icon-512.png    … PWA用アイコン (512x512)
```

---

## 8. 開発ロードマップ

| フェーズ | 内容 | 所要時間目安 |
| --- |---|---|
| 1 | **バックエンド構築** — Supabaseプロジェクト作成、SQL実行（テーブル・RLS・トリガー）、API Credentials取得、Authentication設定 | 15分 |
| 2 | **フロントエンド配置・接続** — ファイル一式配置、`SUPABASE_URL`・`SUPABASE_ANON_KEY` 差し替え | 15分 |
| 3 | **ローカル検証・テスト** — ローカルサーバー起動、登録・ログインテスト、CRUD検証、レスポンシブ確認、オフラインキャッシュテスト | 30分 |
