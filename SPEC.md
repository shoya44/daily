# Daily 仕様書

## 1. 基本方針

Dailyは、日々発生する「小さな判断」「記録」「確認」の手間を減らすための個人用PWAである。

設計上の最優先事項は、機能数を増やすことではなく、以下を減らすこと。

- 判断回数
- タップ数
- 視線移動
- スクロール量
- 保存できたかどうかの不安
- 設定・メンテナンスの手間

**「毎日使っていることを意識しない」ことを目標とする。**

## 2. プロジェクト概要

| 項目 | 内容 |
|---|---|
| アプリ名 | Daily |
| 目的 | 面倒くさがりのための、必要な情報だけを簡単に記録・確認できるカレンダー/手帳風PWA |
| 形式 | PWA（ホーム画面追加可能、オフライン起動対応） |
| 対象デバイス | iPhone（Safari）、PC（Chrome） |
| デザイン | ダーク・ミニマル・Apple/Linearライク |
| データ管理 | Supabase（無料プラン、メールアドレス認証、RLS） |
| 外部API | Open-Meteo API |
| 通知 | Supabase Edge Function + iOSショートカット |

## 3. システム構成

- Frontend: HTML5 / CSS3 / JavaScript（Vanilla JS、ES6+）
- Backend: PostgreSQL（Supabase）
- Authentication: Supabase Auth（Email / Password）
- Weather: Open-Meteo API
- PWA: Web App Manifest + Service Worker
- Hosting: Vercel
- Morning summary: Supabase Edge Function `daily-summary` + iOSショートカット
- 外部通知サービス（OneSignal等）は使用しない

## 4. DB仕様

### 4.1 events

| カラム | 型 | 制約 | 説明 |
|---|---|---|---|
| id | uuid | PK, gen_random_uuid() | 予定ID |
| user_id | uuid | NOT NULL, auth.uid() | ユーザーID |
| date | date | NOT NULL | 予定日 |
| text | text | NOT NULL | 予定・メモ |
| created_at | timestamptz | DEFAULT now() | 作成日時 |

### 4.2 work_records

| カラム | 型 | 制約 | 説明 |
|---|---|---|---|
| id | uuid | PK, gen_random_uuid() | 記録ID |
| user_id | uuid | NOT NULL, auth.uid() | ユーザーID |
| date | date | NOT NULL | 対象日 |
| status | text | NOT NULL, CHECK | 勤務形態 |
| updated_at | timestamptz | DEFAULT now() | 更新日時 |

CHECK値:

`office`, `remote`, `paid_full`, `paid_am`, `paid_pm`, `holiday_work`, `off`

複合UNIQUE制約: `(user_id, date)`

### 4.3 settings

| カラム | 型 | 制約 | 説明 |
|---|---|---|---|
| id | uuid | PK, gen_random_uuid() | 設定ID |
| user_id | uuid | NOT NULL, UNIQUE, auth.uid() | ユーザーID |
| data | jsonb | NOT NULL | ユーザー設定 |

新規ユーザー登録時に `handle_new_user()` トリガーで初期データを生成する。

```json
{
  "workDefaults": {
    "mon": "remote", "tue": "office", "wed": "office",
    "thu": "remote", "fri": "remote", "sat": "off", "sun": "off"
  },
  "garbageSchedule": [
    { "type": "燃えるゴミ", "weekdays": [1, 4], "weeks": [] },
    { "type": "プラスチック", "weekdays": [6], "weeks": [] },
    { "type": "資源", "weekdays": [2], "weeks": [] },
    { "type": "燃えないゴミ", "weekdays": [5], "weeks": [2, 4] }
  ],
  "weatherLocation": { "name": "東京", "lat": 35.68, "lon": 139.76 }
}
```

### 4.4 RLS

全テーブルでRLSを有効化する。

各テーブルの書き込みポリシーは、認証済みユーザーについて以下を満たすこと。

```sql
USING ((select auth.uid()) = user_id)
WITH CHECK ((select auth.uid()) = user_id)
```

目的は、読み取りだけでなくINSERT / UPDATE時にも本人の `user_id` であることを保証すること。

## 5. 画面仕様

### 5.1 認証画面

- メールアドレス・パスワード入力
- ログイン
- 新規登録
- エラー・通知表示
- 認証成功後、自動的にToday画面を表示

### 5.2 Today画面

Today画面をアプリの中心とする。

情報の優先順位は以下。

1. **今日どうする？** — 天気、傘、服装
2. **勤務** — 今日の勤務形態、今週あと何日出社か
3. **ゴミ** — 今日出すゴミ
4. **今日の予定** — 今日の予定・メモ
5. **今週の予定** — 今日より後の予定

#### 天気

- 天気アイコン
- 最高気温
- 天気説明
- 傘の判断
- 服装の判断

単なる状態表示ではなく、可能な限り行動に変換して表示する。

例:

- 「傘必要」ではなく「傘を持っていく」
- 「傘不要」ではなく「傘は不要」

#### 勤務

- 当日の勤務形態を表示
- タップで変更
- 今週の残り出社日数を表示
- 2日以上: 赤、1日: 黄、0日: 緑

#### ゴミ

- 当日出すゴミを表示
- ゴミがある日は見落としにくくする
- ゴミがない日は控えめに表示

#### 予定

- 今日の予定を表示
- タップで編集
- 編集では本文変更・削除が可能
- FAB（＋）から予定追加
- 入力後Enterでも追加できる
- 予定追加画面の入力欄は「何する？」をプレースホルダーとする
- 保存成功時に短いToastを表示する

#### 今週の予定

- 今日より後から週末までの予定を表示
- 日付バッジ付き
- Todayの主情報より視覚的に弱くする

### 5.3 カレンダー画面

- 前月 / 次月
- 今日へ移動
- 月間カレンダー
- 選択日詳細

ドットの意味は固定する。

- 青: 予定あり
- 黄: 勤務変更あり
- 紫: ゴミの日

カレンダーは「入力を複雑にする画面」ではなく「状況を確認する画面」とする。

選択日の詳細には、勤務形態、ゴミ、予定、予定追加を表示する。

### 5.4 設定画面

設定は「一度設定したら普段は触らない画面」とする。

- 勤務形態のデフォルト
- ゴミの日
- 天気取得地点
- アプリ情報 / 最新版を確認
- ログアウト

タグ、カテゴリ、通知設定、複雑なカスタマイズ等は追加しない。

## 6. 機能・判定ロジック

### 6.1 勤務形態

1. 対象日付の `work_records` を検索
2. 明示記録があればその `status` を採用
3. 記録がなければ祝日を確認
4. 祝日なら `off`
5. それ以外は曜日ごとの `workDefaults` を採用

今週の残り出社日数は、今日以降の `office` / `holiday_work` の日数を数える。月境界をまたぐ週にも対応する。

### 6.2 ゴミ

曜日番号（0=日〜6=土）と月内の第N曜日を算出し、`garbageSchedule` の条件に一致する種類を抽出する。

第N曜日:

```text
floor((date - 1) / 7) + 1
```

### 6.3 服装

| 条件 | 表示 |
|---|---|
| 最高気温 >= 28°C | 半袖で快適 |
| 最高気温 >= 22°C | 半袖＋薄手の上着 |
| 最高気温 >= 18°C | 長袖シャツで快適 |
| 最高気温 >= 12°C | 長袖＋カーディガン |
| 最高気温 < 12°C | コートが必要 |

降水確率 >= 50% の場合は傘を持っていくよう強調する。

## 7. エラー処理

### 設定読み込み

「設定が存在しない」と「通信エラー」を区別する。

- データなし → デフォルト設定を使用
- 通信エラー → 既存状態を勝手にデフォルトへ置き換えず、エラー通知

### 初期ロード

設定・月データ・Today画面の読み込み中に例外が発生した場合、コンソールに記録し、ユーザーには短いToastで読み込み失敗を知らせる。

### 書き込み

- オフライン → 変更せずToast表示
- DBエラー → 変更失敗をToast表示
- 成功 → 成功内容を短いToastで表示

## 8. UX原則

### 8.1 判断を減らす

「雨」「出社」などの状態だけでなく、可能な場合は「傘を持っていく」「今週あと2日」のように判断結果を表示する。

### 8.2 操作を減らす

予定追加は、

`＋ → 入力 → Enter`

を基本操作として成立させる。

### 8.3 確認を減らす

不要な確認ダイアログを増やさない。保存成功・削除成功などはToastで知らせる。

### 8.4 情報の強弱を付ける

重要な情報ほど大きく・明確にし、不要な情報は薄くする。

### 8.5 機能を増やさない

利便性向上を目的としても、以下は原則追加しない。

- タグ
- カテゴリ
- 複数カレンダー
- Google Calendar連携
- 高度な検索
- 統計・グラフ
- 複雑な通知設定
- 多ユーザー機能
- 高度なオフライン同期

## 9. オフライン・PWA

- Service Workerで静的アセットをキャッシュ
- オフラインでもアプリ起動・閲覧可能
- オフライン中の書き込みは行わない
- キャッシュ戦略はシンプルなCache Firstを維持
- デプロイ後に古いキャッシュが残る場合は設定画面の「最新版を確認」を使用
- コード変更を伴うデプロイ時は `CACHE_NAME` を更新する

## 10. 朝のサマリー通知

Supabase Edge Function `daily-summary` が当日の情報をJSONで返し、iOSショートカットから取得してローカル通知として表示する。

- 天気
- 服装
- 傘
- 勤務
- ゴミ
- 予定

OneSignal等は使用しない。

Edge FunctionはService Role Key相当の権限を利用するため、ソースはリポジトリに含めない。秘密トークンと `TARGET_USER_ID` はEdge Function側で管理する。

## 11. 祝日データ

`app.js` と `daily-summary` の2箇所に保持する。

年1回、翌々年分を追加し、両方を同期する。低頻度変更のため、共通モジュール化は行わない。

## 12. 非スコープ

- Web Push
- 外部通知サービス
- 複数ユーザー向け機能
- 高度なオフライン編集・同期
- 高度な予定管理
- Google Calendar等との同期
- 複雑なUIカスタマイズ

## 13. 受け入れテスト

- ログイン / 新規登録
- Today画面表示
- 設定なし時のデフォルト設定
- Supabase通信失敗時のエラー表示
- 勤務変更と再読み込み後の保持
- 予定追加 / Enter追加 / 編集 / 削除
- 成功・失敗時のToast
- ゴミの日表示
- 傘・服装判定
- 月境界をまたぐカレンダー
- 今週の残り出社日数
- iPhone SafariでのSafe Area
- PWAホーム画面起動
- オフライン起動
- Service Worker更新
- RLSによるユーザーデータ分離
